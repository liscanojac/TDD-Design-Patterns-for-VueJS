import { createStore } from 'vuex'
import { userData } from './userData'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    userData
  }
})
